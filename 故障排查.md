# 🔧 故障排查指南

## 常见问题和解决方案

### 1. 后端启动问题

#### ❌ 错误：`ModuleNotFoundError: No module named 'flask'`

**原因**：Python依赖未安装

**解决方案**：

**Windows**:
```bash
cd backend
setup_env.bat
```

**Mac/Linux**:
```bash
cd backend
chmod +x setup_env.sh
./setup_env.sh
```

#### ❌ 错误：`ModuleNotFoundError: No module named 'playwright'`

**原因**：Playwright未安装或浏览器未下载

**解决方案**：
```bash
cd backend
source venv/bin/activate  # Mac/Linux
# 或
call venv\Scripts\activate  # Windows

pip install playwright
playwright install chromium
```

#### ❌ 错误：`Address already in use` (端口5000被占用)

**原因**：端口5000已被其他程序占用

**解决方案**：

**Windows**:
```powershell
# 查找占用端口的程序
netstat -ano | findstr :5000
# 结束进程（PID是上面命令显示的数字）
taskkill /F /PID <PID>
```

**Mac/Linux**:
```bash
# 查找并结束占用端口的程序
lsof -ti:5000 | xargs kill -9
```

或修改 `backend/app.py` 最后一行，改用其他端口：
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # 改为5001
```

---

### 2. 前端启动问题

#### ❌ 错误：`npm: command not found`

**原因**：Node.js未安装

**解决方案**：
1. 访问 https://nodejs.org/
2. 下载并安装 LTS 版本（18+）
3. 重启终端后重试

#### ❌ 错误：`ENOENT: no such file or directory, open 'package.json'`

**原因**：不在正确的目录

**解决方案**：
```bash
cd frontend
npm install
npm run dev
```

#### ❌ 错误：`Port 5173 is already in use`

**原因**：前端开发服务器已在运行

**解决方案**：
- 检查是否已有终端在运行 `npm run dev`
- 或者关闭占用端口的进程
- 或者修改 `vite.config.ts`：
```typescript
export default defineConfig({
  server: {
    port: 5174  // 改用其他端口
  }
})
```

---

### 3. AI服务问题

#### ❌ 错误：AI无响应，控制台显示 `Failed to fetch`

**原因**：后端服务未启动或无法连接

**解决方案**：
1. 确认后端已启动：访问 http://localhost:5000/api/health
2. 应该看到：`{"status":"ok",...}`
3. 如果无法访问，检查后端终端是否有错误

#### ❌ 错误：`Invalid API Key`

**原因**：API Key配置错误

**解决方案**：
1. 打开前端"设置"页面
2. 检查API Key格式：
   - OpenAI: 以 `sk-` 开头
   - Anthropic: 以 `sk-ant-` 开头
   - DeepSeek: 以 `sk-` 开头
3. 确保没有多余的空格
4. 重新保存设置

#### ❌ 错误：`Connection timeout` 或 `Network error`

**原因**：无法访问AI服务（可能需要代理）

**解决方案**：

**OpenAI需要代理访问**（中国大陆）：
1. 设置系统代理或VPN
2. 或使用国内中转API：
   - 在"设置"中修改API Endpoint
   - 例如：`https://api.openai-proxy.com/v1`（示例）

**DeepSeek可直连**（推荐国内用户）：
1. 选择AI提供商：DeepSeek
2. API Endpoint: `https://api.deepseek.com/v1`
3. 模型：`deepseek-chat`

---

### 4. BOSS直聘自动化问题

#### ⚠️ 警告：需要人工处理验证码

**原因**：BOSS直聘的反爬虫机制

**解决方案**：
1. **首次登录**：建议手动登录一次
2. **保存Cookie**：登录后Cookie会保存，下次可自动登录
3. **降低频率**：不要过于频繁操作
4. **错峰使用**：避开高峰时段（工作日9-18点）

#### ❌ 错误：`Timeout waiting for selector`

**原因**：页面加载超时或元素定位失败

**解决方案**：
1. 检查网络连接
2. BOSS直聘页面结构可能已更新
3. 需要更新 `backend/boss_automation.py` 中的选择器

#### ❌ 错误：`Browser closed unexpectedly`

**原因**：浏览器崩溃或被强制关闭

**解决方案**：
```bash
cd backend
source venv/bin/activate  # Mac/Linux
# 或 call venv\Scripts\activate  # Windows

# 重新安装浏览器
playwright install chromium --force
```

---

### 5. 配置文件问题

#### ❌ 错误：配置丢失（刷新后消失）

**原因**：浏览器localStorage被清理

**解决方案**：
1. 不要使用无痕模式
2. 不要清理浏览器缓存
3. 可以使用 `backend/.env` 文件存储：
```bash
cd backend
cp .env.example .env
# 编辑 .env 文件
```

#### ❌ 错误：环境变量不生效

**原因**：`.env` 文件格式错误或未加载

**解决方案**：
1. 检查 `.env` 文件格式（每行一个配置，无空格）：
```env
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx
```
2. 确保 `.env` 在 `backend/` 目录下
3. 重启后端服务

---

### 6. 依赖安装问题

#### ❌ 错误：`pip install` 速度很慢

**解决方案**：使用国内镜像

**临时使用**：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**永久配置**：
```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

#### ❌ 错误：`npm install` 速度很慢

**解决方案**：使用国内镜像

**临时使用**：
```bash
npm install --registry=https://registry.npmmirror.com
```

**永久配置**：
```bash
npm config set registry https://registry.npmmirror.com
```

#### ❌ 错误：`Permission denied`

**Mac/Linux解决方案**：
```bash
# 不要用sudo，使用虚拟环境
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

### 7. 性能问题

#### ⚠️ 前端页面卡顿

**解决方案**：
1. 检查浏览器性能（F12 → Performance）
2. 关闭不必要的浏览器标签
3. 升级浏览器到最新版本
4. 清理浏览器缓存

#### ⚠️ 后端响应慢

**解决方案**：
1. 检查AI服务网络延迟
2. 选择更快的AI模型（如gpt-3.5-turbo）
3. 减少并发请求数量
4. 使用国内AI服务（DeepSeek）

---

### 8. 浏览器兼容性

#### 推荐浏览器

✅ **Chrome** 90+（推荐）  
✅ **Edge** 90+  
✅ **Firefox** 88+  
✅ **Safari** 14+  

#### ❌ 不支持的浏览器

❌ IE 11及以下  
❌ Chrome 80以下  

---

### 9. 日志查看

#### 查看后端日志

后端终端会实时显示日志，包括：
- API请求记录
- 错误信息
- 自动化操作状态

#### 查看前端日志

1. 打开浏览器开发者工具：F12
2. 切换到 Console 标签
3. 查看错误信息和网络请求

---

### 10. 完全重置

如果以上方法都不行，可以完全重置：

#### 重置后端
```bash
cd backend
rm -rf venv  # Mac/Linux
# 或 rmdir /s venv  # Windows
rm .env
setup_env.bat  # Windows
# 或 ./setup_env.sh  # Mac/Linux
```

#### 重置前端
```bash
cd frontend
rm -rf node_modules
npm install
```

#### 清理浏览器数据
1. F12 → Application → Local Storage
2. 删除 `localhost:5173` 的所有数据
3. 刷新页面

---

### 获取帮助

如果问题仍未解决：

1. **查看完整文档**：[README.md](README.md)
2. **搜索已知问题**：[GitHub Issues](https://github.com/your-repo/issues)
3. **提交新问题**：提供详细的错误信息和日志
4. **社区讨论**：[GitHub Discussions](https://github.com/your-repo/discussions)

---

### 诊断检查清单

运行以下命令检查环境：

```bash
# 检查Python版本（需要3.8+）
python --version

# 检查Node.js版本（需要18+）
node --version

# 检查npm版本
npm --version

# 检查后端依赖
cd backend
source venv/bin/activate  # Mac/Linux
pip list

# 检查前端依赖
cd frontend
npm list --depth=0

# 测试后端API
curl http://localhost:5000/api/health

# 测试端口占用
netstat -an | grep 5000  # Mac/Linux
netstat -an | findstr 5000  # Windows
```

---

💡 **提示**：大多数问题都是由于依赖未正确安装或网络问题导致的。请先运行 `setup_env` 脚本确保环境正确配置。



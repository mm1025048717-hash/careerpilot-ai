# 🔧 后端环境配置指南

## 问题说明

如果你看到错误：
```
setup_env.bat : The term 'setup_env.bat' is not recognized...
```

这是因为在 PowerShell 中运行 `.bat` 文件需要使用特殊语法。

## ✅ 正确的配置步骤

### 方式一：使用 CMD（推荐，最简单）

1. **打开 CMD**（不是 PowerShell）
   - 按 `Win + R`
   - 输入 `cmd`
   - 回车

2. **进入 backend 目录**
   ```cmd
   cd C:\Users\陈宣任\BOss直聘数字员工\backend
   ```

3. **运行安装脚本**
   ```cmd
   setup_env.bat
   ```

### 方式二：在 PowerShell 中运行（需要特殊语法）

如果你必须使用 PowerShell：

```powershell
cd C:\Users\陈宣任\BOss直聘数字员工\backend
.\setup_env.bat
```

**注意**：前面要加 `.\`（点和反斜杠）

### 方式三：双击运行（最简单）

1. 打开文件资源管理器
2. 进入 `C:\Users\陈宣任\BOss直聘数字员工\backend`
3. **双击 `setup_env.bat`**
4. 等待安装完成

## 🚀 快速配置（推荐流程）

### Step 1: 安装后端依赖

**双击运行**：`backend\setup_env.bat`

或在 CMD 中：
```cmd
cd backend
setup_env.bat
```

等待看到：
```
✅ 环境配置完成！
```

### Step 2: 测试 DeepSeek API

1. 先创建 `.env` 文件：

在 `backend` 目录下创建文件 `.env`，内容：

```env
DEEPSEEK_API_KEY=sk-1203fb58062a43fdad35082e9b0aa8c3
DEEPSEEK_API_BASE=https://api.deepseek.com
```

2. 测试连接（CMD 中运行）：

```cmd
cd backend
venv\Scripts\activate
python test_deepseek.py
```

如果看到：
```
✅ 连接成功！
AI 回复：...
```

说明 DeepSeek 配置成功！

### Step 3: 启动项目

**双击运行**：`启动项目.bat`

或分别启动：
1. 双击 `backend\run.bat`
2. 双击 `frontend\run.bat`

### Step 4: 在浏览器中配置

1. 打开：http://localhost:5173
2. 点击左侧"设置"
3. 填写：
   ```
   AI提供商: DeepSeek
   API Key: sk-1203fb58062a43fdad35082e9b0aa8c3
   API Endpoint: https://api.deepseek.com
   模型: deepseek-chat
   ```
4. 点击"保存设置"
5. 切换到"AI对话"，输入"你好"测试

## 🐛 如果还是有问题

### 问题1：Python 未安装

**检查**：
```cmd
python --version
```

如果提示"不是内部或外部命令"，说明 Python 未安装或未添加到 PATH。

**解决**：
1. 访问 https://www.python.org/downloads/
2. 下载 Python 3.8+ 安装包
3. **重要**：安装时勾选 "Add Python to PATH"
4. 安装完成后重启电脑

### 问题2：pip 安装速度慢

在 `backend` 目录下运行（CMD）：

```cmd
venv\Scripts\activate
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip install -r requirements.txt
playwright install chromium
```

### 问题3：权限问题

以管理员身份运行 CMD：
1. 按 `Win` 键，搜索 "cmd"
2. 右键点击"命令提示符"
3. 选择"以管理员身份运行"
4. 然后再执行安装命令

## 📝 完整的环境配置清单

```cmd
# 1. 检查 Python
python --version

# 2. 进入项目目录
cd C:\Users\陈宣任\BOss直聘数字员工

# 3. 安装后端依赖
cd backend
setup_env.bat

# 4. 创建 .env 文件（复制下面内容到 backend\.env）
# DEEPSEEK_API_KEY=sk-1203fb58062a43fdad35082e9b0aa8c3
# DEEPSEEK_API_BASE=https://api.deepseek.com

# 5. 测试 DeepSeek
venv\Scripts\activate
python test_deepseek.py

# 6. 启动项目（在项目根目录）
cd ..
启动项目.bat
```

## ✅ 验证清单

- [ ] Python 3.8+ 已安装
- [ ] 后端依赖已安装（`backend\venv` 文件夹存在）
- [ ] `.env` 文件已创建并填入 API Key
- [ ] `test_deepseek.py` 测试通过
- [ ] 后端服务启动成功（http://localhost:5000/api/health）
- [ ] 前端服务启动成功（http://localhost:5173）
- [ ] 前端"设置"中已配置 DeepSeek
- [ ] AI 对话测试通过

## 💡 快捷方式（跳过所有命令行）

1. **双击** `backend\setup_env.bat`（等待完成）
2. 用记事本打开 `backend\.env.example`
3. 另存为 `backend\.env`
4. 修改内容：
   ```
   DEEPSEEK_API_KEY=sk-1203fb58062a43fdad35082e9b0aa8c3
   ```
5. **双击** `启动项目.bat`
6. 浏览器打开 http://localhost:5173
7. 在"设置"中配置 DeepSeek
8. 开始使用！

---

💡 **提示**：建议使用 CMD 而不是 PowerShell 来运行 `.bat` 文件，这样不会有兼容性问题。


